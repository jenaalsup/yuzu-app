rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /parties/{partyId} {
      // Temporarily allow all operations if authenticated
      allow read, write: if request.auth != null;
    }
  }
}